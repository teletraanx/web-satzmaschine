// Pronoun.cpp
#include "Pronoun.h"

Pronoun::Pronoun(string kind, string level, string english, string word, string number, string person) : kind(kind), level(level), english(english), word(word), number(number), person(person) {} // constructor
